# Leaf Link - Tea Leaves Collection and Delivery Management System

## Development Steps

1. Install Docket
2. Run Docker Daemon
3. type the command below in the terminal

```CLI
  docker compose up --build
```
